#include "Scene.h"
#include <memory>


namespace olivera

{

  void Scene::onInitialise(int _windowWidth, int _windowHeight)
  {
    windowWidth = _windowWidth;
    windowHeight = _windowHeight;

  }
  void Scene::sceneSetup()
  {

  }
}